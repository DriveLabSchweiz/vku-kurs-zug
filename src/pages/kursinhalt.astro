---
import BaseLayout from '../layouts/BaseLayout.astro';
import vkuData from '../data/vku_data.json';

const { course } = vkuData;
---

<BaseLayout
  title="VKU Kursinhalt – Die 4 Module erklärt"
  description="Erfahren Sie mehr über die 4 VKU-Module: Verkehrssehen, Verkehrsumwelt, Verkehrsdynamik und Verkehrstaktik."
  canonical="https://vku-kurs-zug.ch/kursinhalt"
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-12">
    <div class="max-w-6xl mx-auto px-4">
      <h1 class="text-4xl font-bold mb-4">VKU Kursinhalt</h1>
      <p class="text-xl">Die 4 Module des Verkehrskundeunterrichts</p>
    </div>
  </section>

  <!-- Breadcrumb -->
  <nav class="bg-gray-50 py-4">
    <div class="max-w-6xl mx-auto px-4">
      <a href="/" class="text-blue-600 hover:underline">Home</a> > <span class="text-gray-600">Kursinhalt</span>
    </div>
  </nav>

  <!-- Main Content -->
  <section class="py-12 max-w-6xl mx-auto px-4">
    <p class="text-lg text-gray-700 mb-8">
      Der VKU besteht aus 4 aufeinander aufbauenden Modulen, die zusammen ein umfassendes Verständnis für sicheres Fahren vermitteln. Jedes Modul behandelt einen wichtigen Aspekt der Verkehrskunde.
    </p>

    {course.modules.map((module, index) => (
      <div class="mb-12">
        <div class="flex items-center mb-4">
          <div class="bg-blue-600 text-white rounded-full w-12 h-12 flex items-center justify-center font-bold text-lg mr-4">
            {index + 1}
          </div>
          <h2 class="text-3xl font-bold">{module.title}</h2>
        </div>
        <div class="bg-blue-50 p-8 rounded">
          <p class="text-lg text-gray-700 leading-relaxed">{module.description}</p>
        </div>
      </div>
    ))}

    <!-- Additional Info -->
    <div class="mt-12 p-8 bg-gray-50 rounded">
      <h3 class="text-2xl font-bold mb-4">Warum diese Module?</h3>
      <p class="text-gray-700 mb-4">
        Die 4 Module des VKU sind gesetzlich vorgeschrieben und basieren auf wissenschaftlichen Erkenntnissen zur Verkehrssicherheit. Sie vermitteln nicht nur theoretisches Wissen, sondern auch praktische Fähigkeiten für sicheres Fahren.
      </p>
      <p class="text-gray-700">
        Durch die Kombination dieser Module lernst du, Verkehrssituationen frühzeitig zu erkennen, angemessen zu reagieren und Risiken zu minimieren.
      </p>
    </div>

    <!-- CTA -->
    <div class="mt-12 text-center">
      <a href="/" class="bg-blue-600 text-white px-8 py-3 rounded font-bold hover:bg-blue-700 inline-block">Zurück zur Startseite</a>
      <a href="/kontakt" class="bg-gray-600 text-white px-8 py-3 rounded font-bold hover:bg-gray-700 inline-block ml-4">Jetzt anmelden</a>
    </div>
  </section>

  <!-- Breadcrumb Schema -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Home",
        "item": "https://vku-kurs-zug.ch/"
      },
      {
        "@type": "ListItem",
        "position": 2,
        "name": "Kursinhalt",
        "item": "https://vku-kurs-zug.ch/kursinhalt"
      }
    ]
  }
  </script>
</BaseLayout>
